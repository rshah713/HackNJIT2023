#: import rgba kivy.utils.rgba
#: import Colors colors.Colors
<EMSDashboard>:
    name: 'ems dashboard'
    patname: patname
    patage: patage
    patheight: patheight
    patweight: patweight
    patBT: patBT
    patID: patID
    patMedCond: patMedCond
    patAllergies: patAllergies
    patMedication: patMedication
    GridLayout:
        cols: 1

        Label:
            text: "Patient Info"
            font_size: min(self.width*0.3, self.height) * 0.45
            font_name: 'Roboto-Bold'
            color: rgba(Colors.BLUE)

####################### name+age ##################################

        GridLayout:
            cols: 2
            size_hint: (.7, .2)

            Label:
                id: patname
                size_hint: (.7, .2)
                pos_hint: {'x': 0.15}
                text: "Name: "
                font_size: min(self.width*0.2, self.height) * 0.6
                color: rgba(Colors.BLUE)
                foreground_color: rgba(Colors.BLUE)

            Label:
                id: patage
                size_hint: (.7, .2)
                pos_hint: {'x': 0.15}
                text: "Age: "
                font_size: min(self.width*0.2, self.height) * 0.6
                color: rgba(Colors.BLUE)
                foreground_color: rgba(Colors.ACCENT_BLUE)

        Widget:
            size_hint_y: 0.02

####################### weight+height ##################################
        GridLayout:
            cols: 2
            size_hint: (.7, .2)

            Label:
                id: patweight
                pos_hint: {'x': 0.15}
                text: "Weight: "
                font_size: min(self.width*0.2, self.height) * 0.6
                color: rgba(Colors.BLUE)
                background_color: rgba(*Colors.BLUE, 0)
                foreground_color: rgba(Colors.ACCENT_BLUE)

            Label:
                id: patheight
                pos_hint: {'x': 0.15}

                text: "Height: "
                font_size: min(self.width*0.2, self.height) * 0.6
                color: rgba(Colors.BLUE)
                color: rgba(Colors.BLUE)
                foreground_color: rgba(Colors.ACCENT_BLUE)

        Widget:
            size_hint_y: 0.02

####################### BT+ID ##################################
        GridLayout:
            cols: 2
            size_hint: (.7, .2)

            Label:
                id: patBT
                pos_hint: {'x': 0.15}
                text: "Blood Type: "
                font_size: min(self.width*0.2, self.height) * 0.6
                color: rgba(Colors.BLUE)
                foreground_color: rgba(Colors.ACCENT_BLUE)

            Label:
                id: patID
                pos_hint: {'x': 0.15}
                text: "DLID: "
                font_size: min(self.width*0.2, self.height) * 0.6
                color: rgba(Colors.BLUE)
                foreground_color: rgba(Colors.ACCENT_BLUE)

        Widget:
            size_hint_y: 0.02

####################### Conditions ##################################
        Label:
            id: patMedCond
            pos_hint: {'x': 0.15}
            text: "Medical Conditions: "
            font_size: min(self.width*0.3, self.height) * 0.15
            color: rgba(Colors.BLUE)
            foreground_color: rgba(Colors.ACCENT_BLUE)

        Widget:
            size_hint_y: 0.02

####################### Allergies ##################################
        Label:
            id: patAllergies
            pos_hint: {'x': 0.15}
            text: "Allergies: "
            font_size: min(self.width*0.3, self.height) * 0.15
            color: rgba(Colors.BLUE)
            foreground_color: rgba(Colors.ACCENT_BLUE)

        Widget:
            size_hint_y: 0.02

####################### Medications ##################################
        Label:
            id: patMedication
            pos_hint: {'x': 0.15}
            text: "Medications: "
            font_size: min(self.width*0.3, self.height) * 0.15
            color: rgba(Colors.BLUE)
            foreground_color: rgba(Colors.ACCENT_BLUE)